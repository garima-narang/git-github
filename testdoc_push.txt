git init

git remote add origin " link from github clone url       "

git pull origin master

git status

git add <filename>

git add -A     /// add aall files in one go 

git rm <file name>

git commit -m "msg"

git commit -a -m "msg"
 git log

creating branches

git branch <newbranch name>


git checkout <branchname> // checout from prev n checkin to new branch

files made in new branch will not be reflected in master branch
 
